/* 
// Display every element of an array, using the 'for' loop.
var numbers = [45, 87, 89, 56, 32, 51, 25];

for (var i = 0; i < 7; i++) {
    // console.log(i);
    var number = numbers[i];
    console.log(number);
} 
*/

/* 
// The above same task (displaying every elements of an array), using the 'while' loop.
var numbers = [45, 87, 89, 56, 32, 51, 25];

var indexNumber = 0;

while (indexNumber < 7) {
    var number = numbers[indexNumber];
    console.log(number);
    indexNumber++;
} 
*/

/* 
// Output ||
45
87
89
56
32
51
25
*/



/* 
// Display every element of an array, using the 'for' loop [using the condition of unlimited array-length].
var numbers = [45, 87, 89, 56, 32, 51, 25, 188, 41];

for (var i = 0; i < numbers.length; i++) {
    // console.log(i);
    var number = numbers[i];
    console.log(number);
}  
*/

/* 
// The above same task using the 'while' loop.
var numbers = [45, 87, 89, 56, 32, 51, 25, 188, 41];

var indexNumber = 0;

while (indexNumber < numbers.length) {
    var number = numbers[indexNumber];
    console.log(number);
    indexNumber++;
} 
*/

/* 
// Output ||
45
87
89
56
32
51
25
188
41 
*/



/* 
// Display every element of an array, using the 'for' loop [using the condition of unlimited array-length and string-elements of the array]
var items = ["bottle", "mouse", "sunglass", "pen", "notebook"];

for (var i = 0; i < items.length; i++) {
    var item = items[i];
    console.log(item);
} 
*/

/* 
// The above same task using the 'while' loop.
var items = ["bottle", "mouse", "sunglass", "pen", "notebook"];

var indexNumber = 0;

while (indexNumber < items.length) {
    var item = items[indexNumber];
    console.log(item);
    indexNumber++
} 
*/

/* 
// Output ||
bottle
mouse
sunglass
pen
notebook 
*/